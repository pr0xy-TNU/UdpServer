

import static utils.LoggerStatus.INFO;
import static utils.LoggerStatus.WARNING;
import static utils.NetHelper.logger;
import utils.LoggerStatus;

public class Main {

    public static void main(String[] args) {
        logger(WARNING, "hello");
    }
}
